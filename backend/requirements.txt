# Core dependencies
# Critical packages - pin exact versions for stability
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic[email]==2.12.5
sqlalchemy==2.0.45

# Security-sensitive packages - allow patch updates
requests>=2.32.5,<3.0.0
urllib3>=2.6.3,<3.0.0
PyJWT>=2.10.1,<3.0.0

# Non-critical packages - allow minor updates
python-multipart>=0.0.21,<1.0.0
zstandard>=0.23.0,<1.0.0
aiosqlite>=0.22.1,<1.0.0
click>=8.3.1,<9.0.0

# Testing dependencies
# Pinned for test stability - update carefully
pytest==9.0.3
pytest-asyncio==1.3.0
httpx>=0.28.1,<1.0.0

# Optional database drivers - install based on your database choice:
# SQLite (default) - no additional drivers needed (built into Python)

# PostgreSQL:
psycopg2-binary>=2.9.11,<3.0.0

# MySQL:
PyMySQL>=1.1.2,<2.0.0

# MariaDB:
# PyMySQL>=1.0.0 (same as MySQL)

# Oracle:
# cx_Oracle>=8.0.0

# Microsoft SQL Server:
# pyodbc>=4.0.0
# Note: Also requires ODBC Driver 17 for SQL Server to be installed on the system

# Additional optional dependencies for production:
python-dotenv>=1.2.1,<2.0.0  # For .env file support
python-json-logger>=2.0.7,<3.0.0  # Structured JSON logging
# cryptography>=3.0.0   # For enhanced SSL/TLS support
aiohttp>=3.13.3,<4.0.0
google-auth>=2.0.0,<3.0.0  # Google OAuth ID token verification
redis[hiredis]>=5.0.0,<6.0.0  # Async Redis client (hiredis for C parser speed)
psutil>=7.2.1,<8.0.0
colorama>=0.4.6,<1.0.0
Jinja2>=3.1.0,<4.0.0  # For email templates

# TikTok-bot module
TikTokLive>=6.4,<7.0.0     # WebCast WebSocket listener for the read pipeline

# Development dependencies (linting, type checking, etc.)
ruff>=0.6.0,<1.0.0         # Fast Python linter and formatter
mypy>=1.8.0,<2.0.0         # Static type checker for Python
